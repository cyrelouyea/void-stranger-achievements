title: Do not hurt them
description: Talk to the lonely one in E010

// PATCH gml_Object_obj_player_Step_0 1232
if special_message == 82 && ds_grid_get(obj_inventory.ds_player_info, 22, 1) != 1000
{+achievement}