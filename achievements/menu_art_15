title: Menu Art (Mimic Gray)
description: Look at the Mimic Gray art in the pause menu.

// PATCH gml_Object_obj_menu_Step_0 21
if ds_grid_get(ds_, 3, menu_option[page]) == spr_menu_continue_mirror
    {+achievement}