title: V1000
description: Use the void rod 1000 times in a single room

// PATCH gml_Object_obj_player_Step_0 1144
if ds_grid_get(obj_inventory.ds_player_info, 3, 1) >= 1000
    {+achievement}
// PATCH gml_Object_obj_player_Step_0 825
if ds_grid_get(obj_inventory.ds_player_info, 3, 1) >= 1000
    {+achievement}
