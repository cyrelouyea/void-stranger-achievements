title: Counter-terrorist (+)
description: Fight and kill Mon in less than 50 steps. The step counter (F3) will be the reference.

// PATCH gml_Object_obj_enemy_mon_head_Step_0 457
if ds_grid_get(obj_inventory.ds_player_info, 3, 0) < 50
{+achievement}