title: They mean no harm
description: Talk to the lonely one in E008

// PATCH gml_Object_obj_player_Step_0 1232
if special_message == 80 && ds_grid_get(obj_inventory.ds_player_info, 22, 1) != 1000
{+achievement}