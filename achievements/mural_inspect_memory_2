title: ONLYACRAVINGREMAINS
description: Decipher the mural in the third domain

// PATCH gml_Object_obj_mural_Step_0 646
if inscription == 2 && ds_grid_get(obj_inventory.ds_equipment, 0, 0) != 0 && global.memory_toggle
{+achievement}