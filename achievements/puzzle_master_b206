title: Puzzle master (B206)
description: Solve B206 with Lillie by taking the stairs and without using the sword, the wings or the upgraded rod.

// PATCH gml_Object_obj_exit_Alarm_0 40
if global.loop == 1 && room == rm_0211 && !global.endless_used && !global.sword_used && !global.wings_used
{+achievement}