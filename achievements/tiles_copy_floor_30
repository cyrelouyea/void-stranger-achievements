title: *Bwooop* (30/1000)
description: Spawn a total of 30 shades with copy tiles

// PATCH gml_Object_obj_enemy_cr_Create_0 1
if obj_inventory.copy_floor_counter >= 30
    {+achievement}

// PATCH gml_Object_obj_enemy_cr_segment_Create_0 1
if obj_inventory.copy_floor_counter >= 30
    {+achievement}
